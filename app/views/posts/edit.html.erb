
<%= render partial: 'error', locals: { post:@post } %>

<h1>投稿編集</h1>

<%= form_with model: @post, url: post_path(@post.id), method: :patch do |f| %>

  <h4>タイトル</h4>
  <%= f.text_field :title %>

  <h4>質問内容</h4>
  <%= f.text_area :body %>

  <%= f.submit '投稿' %>
<% end %>