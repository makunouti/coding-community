<h1>質問内容フォーム</h1>

<%= render 'error', obj: @question %>

<%= form_with model: @question do |f| %>
  <h4>タイトル</h4>
  <%= f.text_field :title %>

  <h4>質問内容</h4>
  <div><%= f.text_area :body %></div>

  <%= f.submit '投稿' %>
<% end %>

<%= link_to '戻る', questions_path %>